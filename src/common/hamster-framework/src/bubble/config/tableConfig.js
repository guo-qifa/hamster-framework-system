import { Slider } from "element-ui";
import floatObj from "../lib/floatObj"

/**
 * 参数说明
 * text: 显示名称
 * key: 取值字段
 * dictionary: 所用字典
 * color: Object[key: 值, value: 值对应的颜色(使用预设class)]
 * render: Function(当前数据, 当前字段值)
 * plugs: 当前字段所用插件
 * ellipsis: 字段过长时显示多少字符
 * type: 字段类型[text, date]
 * emptyText: 无数据时显示内容,默认为暂无
 */

/**
 * 预设颜色class
 * text-success
 * text-warning
 * text-danger
 * text-info
 */
var tableConfig = {
    
}

export default tableConfig;